# Hero restore point: solo portrait

Saved before testing the meeting/handshake hero on 2026-07-16.

This folder preserves the complete previous treatment:

- `page.tsx.txt` — previous hero markup and image references
- `globals.css.txt` — previous hero positioning, masking and responsive treatment
- `redstone-hero-background.png` — clean office background plate
- `redstone-hero-man-cutout.png` — transparent solo subject layer

To restore only the hero, copy the hero markup and related `.hero*` rules from these files and restore the two image assets to `public/` if they have been removed.
